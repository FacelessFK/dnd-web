node_modules
.pnpm-store

# Environment files
.env
.env.*
!.env.example
!**/.env.example

# Build output
.next
dist
coverage
*.tsbuildinfo
apps/server/data

# Logs
*.log
*.zip
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS/editor junk
.DS_Store
.idea
.vscode
.codex

# Private rules references and raw art drops stay local. The 2014 Player's
# Handbook PDF is copyrighted and must never be committed - see README.md,
# "Rules source policy".
local-reference/
apps/web/.visual-capture/

# Blender working files. The export pipeline that produces them lives on
# chore/blender-asset-toolchain; the binaries are never source.
assets-source/**/*.blend
assets-source/**/*.blend1
